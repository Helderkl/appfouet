# Backend API URL (leave empty to use /api proxy in development)
VITE_API_URL=

# NOTE: All sensitive credentials (DB connection, JWT secret, Focus NFe API key,
# certificate passwords) MUST be stored in the BACKEND .env only.
# The frontend should NEVER contain secrets.
